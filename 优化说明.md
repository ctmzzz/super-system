# 服务器优化说明

## 完成的优化工作

### 1. 教师账号单设备登录限制
- **修改内容**：将教师账号的登录逻辑修改为与班级账号一致
- **功能说明**：
  - 教师账号同时只允许1个设备在线
  - 第二个设备尝试登录时会被拒绝，并提示"该账号已有1个设备在线，请先退出一个设备后再登录"
  - WebSocket连接时也会进行相同的检查
- **修改文件**：
  - `server.js` - 更新了登录验证逻辑
  - `ws-server.js` - 添加了`TEACHER_MAX_SESSIONS`常量，更新了WebSocket连接逻辑

### 2. 服务器资源实时监控系统
- **功能说明**：
  - 运行在 `3002` 端口
  - 需要管理员账号登录才能访问
  - 支持服务器IP输入（默认自动填充当前地址）
  - 即使数据库连接失败也能正常进入监控页面，会显示"服务器已脱机"
  - 实时显示CPU使用率、内存使用率、在线用户数、异常账号数等
  - 实时显示服务器日志
  - 攻击检测功能
- **新增文件**：
  - `monitor-server.js` - 监控系统后端
  - `monitor-public/index.html` - 监控页面
  - `monitor-public/style.css` - 监控页面样式
  - `monitor-public/app.js` - 监控页面脚本

### 3. PM2集群模式支持
- **功能说明**：
  - 使用PM2的集群模式充分利用CPU多核性能
  - 主应用会自动根据CPU核心数启动对应数量的实例
  - 监控系统以单实例模式运行
- **新增/修改文件**：
  - `ecosystem.config.js` - PM2配置文件
  - `package.json` - 添加了PM2相关依赖和启动脚本

### 4. MySQL连接池优化
- **优化内容**：
  - 连接池大小从10增加到50
  - 队列限制设置为100
  - 添加连接超时和保持连接配置
  - 提高并发处理能力
- **修改文件**：
  - `database-mysql.js` - 更新了数据库连接配置

## 使用方法

### 安装依赖
```bash
npm install
```

### 开发模式运行

#### 仅运行主应用
```bash
npm start
```

#### 仅运行监控系统
```bash
npm run monitor
```

#### 同时运行主应用和监控系统
```bash
npm run start:all
```

### PM2生产模式运行

#### 安装PM2（如果尚未安装）
```bash
npm install -g pm2
```

#### 使用PM2启动
```bash
npm run pm2:start
```

#### 查看状态
```bash
npm run pm2:status
# 或者
pm2 status
```

#### 查看日志
```bash
npm run pm2:logs
# 或者
pm2 logs
```

#### 停止服务
```bash
npm run pm2:stop
```

#### 重启服务
```bash
npm run pm2:restart
```

#### 删除服务
```bash
npm run pm2:delete
```

## 访问地址

- **主应用**：http://localhost:3001 （或您的服务器IP:3001）
- **监控系统**：http://localhost:3002 （或您的服务器IP:3002）

## 注意事项

1. 监控系统的管理员账号与主系统的管理员账号一致
2. 默认的备用管理员账号：
   - 用户名：admin，密码：admin123
   - 用户名：root，密码：root
3. 请确保在使用PM2前已全局安装PM2
4. 数据库（192.168.3.6）需要保持运行状态以获得完整功能
